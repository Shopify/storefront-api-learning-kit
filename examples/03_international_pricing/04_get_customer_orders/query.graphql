query getcustomerOrders ($customerAccessToken: String!) @inContext(country: FR) {
  customer(customerAccessToken: $customerAccessToken) {
    orders(first:10) {
      edges {
        node {
          totalPrice {
            amount
            currencyCode # store's currency - CAD in this case
          }
          lineItems(first:10) {
            edges {
              node {
                originalTotalPrice {
                  amount
                  currencyCode # store's currency - CAD in this case
                }
                variant {
                  price {
                    amount
                    currencyCode # EUR currency (if French market enabled for store)
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
